{
  "name": "tweety-backend",
  "version": "1.0.0",
  "description": "Backend server for Technical Thread Generator",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "clean": "rm -rf dist",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.56.0",
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0",
    "dotenv": "^17.2.0",
    "node-fetch": "^3.3.2",
    "ollama": "^0.5.0",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "tsx": "^4.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
